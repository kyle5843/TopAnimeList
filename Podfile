# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TopAnimeList' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TopAnimeList
  pod 'RxSwift', '~> 5.1'
  pod 'RxCocoa', '~> 5.1'
  pod 'Alamofire', '~> 5.2'
  pod 'SDWebImage', '~> 5.0'

  target 'TopAnimeListTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TopAnimeListUITests' do
    # Pods for testing
  end

end
